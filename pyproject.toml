[project]
name = "futarin-led"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "flask>=3.0.3",
    "rpi-ws281x>=5.0.0",
]

[tool.uv.workspace]
members = ["app"]
